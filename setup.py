from setuptools import setup, find_packages

setup(
    name="ema",
    version='0.1.a1',
    install_requires=["pysam", 
                      "anndata", 
                      "scanpy", 
                      "pandas", 
                      "scipy"]
    
)
